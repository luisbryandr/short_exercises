function firstToLast(str,c){
    let first = str.indexOf(c)
    let last = str.lastIndexOf(c)
    return first === -1 ? first : last - first
}

//nice to learn and see that co pilot can do this so easily and withouth prompting
// still learning the fundamentals of JS and how to use it but its good to know that I don't have to memorize everything and that I can use co pilot to help me
